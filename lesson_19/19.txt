✅ Створіть власний Python модуль, який містить функцію для обчислення суми чисел у списку. Імпортуйте цей модуль і використовуйте функцію для знаходження суми чисел у заданому списку.

✅ Створіть функцію, яка приймає два аргументи - дату народження та поточну дату. Функція має обчислити вік особи на момент поточної дати.

✅ Використовуючи бібліотеку requests, виведіть на екран вміст сторінки "https://www.example.com".

✅ Напишіть програму, яка приймає від користувача два числа та вибір операції (додавання, віднімання, множення, ділення). Виконайте вибрану операцію та виведіть результат.

✅ Створіть функцію, яка приймає текстовий рядок та повертає кількість слів у цьому рядку.

✅ Використовуючи бібліотеку shutil, створіть програму, яка скопіює вміст одного текстового файлу в інший файл.

✅ Напишіть функцію, яка приймає на вхід список чисел і повертає список, де кожний елемент є квадратом відповідного числа в початковому списку.

✅ Виведіть поточний час та дату з використанням модулю datetime.

✅ Створіть програму, яка приймає два файли та об'єднує їх в один.

✅ Використовуючи бібліотеку argparse, створіть програму, яка може обчислювати площу прямокутника або трикутника, залежно від переданих аргументів.

✅ Напишіть функцію, яка перевіряє, чи є рядок паліндромом (читається однаково зліва направо та справа наліво).

✅ Виведіть інформацію про поточну операційну систему з використанням модулю os.

✅ Створіть програму, яка перевіряє, чи існує файл або каталог з вказаним ім'ям у поточному каталозі.

✅ Використовуючи модуль random, напишіть гру "Камінь, ножиці, папір" проти комп'ютера.

✅ Створіть програму, яка перетворює рядок на "заголовний регістр" (великі літери).

✅ Завантажте зображення з Інтернету за допомогою бібліотеки requests та відобразіть його на екрані.

✅ Створіть програму, яка приймає два числа та визначає, чи є вони взаємно простими (не мають спільних дільників, крім 1).

✅ Напишіть функцію, яка визначає, чи є заданий рік високосним.

✅ Використовуючи бібліотеку argparse, створіть програму, яка приймає шлях до файлу та визначає його розмір.

✅ Створіть програму, яка перетворює температуру з градусів Цельсія в градуси Фаренгейта та навпаки.

✅ Виведіть всі файлові розширення у поточному каталозі.

✅ Створіть програму, яка знаходить середнє арифметичне чисел у списку.

✅ Виконайте HTTP-POST-запит до віддаленого сервера та виведіть відповідь на екран.

✅ Напишіть функцію, яка визначає, чи є задане число простим.

✅ Створіть програму, яка обчислює факторіал числа.

✅ Виконайте HTTP-GET-запит до віддаленого сервера та виведіть заголовок відповіді на екран.

✅ Завантажте зображення з Інтернету за допомогою бібліотеки requests та збережіть його на диск.

Створіть функцію, яка генерує випадковий пароль заданої довжини.

✅ Напишіть програму, яка перевіряє, чи є заданий рядок анаграмою іншого рядка.

Використовуючи бібліотеку shutil, створіть програму, яка видаляє всі файли з певного каталогу, які старше заданої дати.