# Напишіть функцію, яка приймає на вхід список чисел і повертає список, де кожний елемент є квадратом відповідного
# числа в початковому списку.
def pow_in_lst(lst):
    return list(map(lambda x: x ** 2, lst))


lst = [1, 2, 3, 4, 5]

print(pow_in_lst(lst))
