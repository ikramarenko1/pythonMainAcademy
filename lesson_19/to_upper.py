# Створіть програму, яка перетворює рядок на "заголовний регістр" (великі літери).
def to_upper(string):
    return string.upper()


string = input('Введiть рядок для переведння в верхнiй регiстр: ')

print(to_upper(string))
